import type { RepoHighlight } from "./types";

/**
 * EDIT ME: Manually list repositories you want to showcase, instead
 * of pulling live from the GitHub API (keeps this static, fast, and
 * free of tokens). Leave empty until you have public repos to link.
 *
 * Example:
 * {
 *   name: "clearance-system",
 *   description: "Digital exam clearance system for Caritas University.",
 *   technologies: ["React", "TypeScript", "Supabase"],
 *   stars: 0,
 *   forks: 0,
 *   githubUrl: "https://github.com/perristtech.dev/clearance-system",
 *   liveUrl: null,
 * }
 */
export const repoHighlights: RepoHighlight[] = [];
