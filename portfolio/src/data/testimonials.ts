import type { Testimonial } from "./types";

/**
 * EDIT ME: Add real client/collaborator testimonials only. Leave
 * empty (`[]`) to hide the section — never fabricate quotes.
 */
export const testimonials: Testimonial[] = [];
